# ============================================================================
# HomeGameServer - Simplified Configuration
# ============================================================================
# A lightweight, local-first game server for home/LAN gaming
# No authentication required - just pick a username and play!

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# Port to listen on (default: 8081)
PORT=8081

# Node environment: development, production, test
NODE_ENV=development

# -----------------------------------------------------------------------------
# Game Server Configuration
# -----------------------------------------------------------------------------

# Game tick rate (updates per second, default: 30)
GAME_TICK_RATE=30

# Snapshot interval in milliseconds (default: 100)
SNAPSHOT_INTERVAL=100

# Maximum players per room (default: 8)
MAX_PLAYERS_PER_ROOM=8

# -----------------------------------------------------------------------------
# Optional Features
# -----------------------------------------------------------------------------

# Enable metrics endpoint (default: false)
ENABLE_METRICS=false

# Metrics access token (optional, for /metrics endpoint)
# METRICS_TOKEN=

# Enable debug logging (default: false)
DEBUG=false

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# - No secrets required! This is a local-first server
# - User data is stored in data/users.json
# - All settings are optional with sensible defaults
# - For local gaming, the defaults work great!
